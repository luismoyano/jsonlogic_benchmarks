# Downloaded test suites (fetched fresh on each CI run)
tests/

# Ruby
*.gem
Gemfile.lock

# PHP — lock file and vendor are intentionally excluded so CI always
# resolves the latest published versions of each library
benchmark_php/composer.lock
benchmark_php/vendor/

# Local development scripts (not for CI)
benchmark_ruby/local_benchmark.rb

# OS
.DS_Store

# Editor/IDE (except .idea which has project config)
*.swp
*~

# JetBrains IDEs (RubyMine, WebStorm, etc.) project files
.idea/
